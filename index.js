const calculate = require('./src/calculator');
const { buyerTypes, countries, propertyTypes } = require('./src/config');

module.exports = {
  calculate,
  buyerTypes,
  countries,
  propertyTypes
};
